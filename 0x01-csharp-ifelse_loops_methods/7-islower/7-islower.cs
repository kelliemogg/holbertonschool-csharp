using System;

class Character
{
    public static bool IsLower(char c)
    {
        bool result = true;
        bool result2 = false;
        if (c >= 'a' && c <= 'z')
            return (result);
        else
            return (result2);
    }
}
